# Workflow Status Template

# This tracks progress through BMM methodology Analysis, Planning, and Solutioning phases.
# Implementation phase is tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2026-02-10"
project: "FB_APU01"
project_type: "SaaS multi-tenant para apuração de IBS/CBS - FASE 2 do fbtax.cloud"
selected_track: "enterprise"
field_type: "brownfield"
workflow_path: "/home/claudiobezerra/projetos/FB_APU01/_bmad/bmm/workflows/workflow-status/paths/enterprise-brownfield.yaml"
workflow_status:
  # PHASE 0: Documentation (Conditional - brownfield)
  - phase: "0"
    name: "Documentation"
    id: "document-project"
    status: "optional"
    note: "Project already has technical documentation in docs/ - can be skipped or updated"
    agent: "analyst"
    command: "/bmad:bmm:workflows:document-project"

  # PHASE 1: Analysis (Optional - Discovery)
  - phase: "1"
    name: "Analysis (Optional)"
    workflows:
      - id: "brainstorm-project"
        status: "required"
        note: "Creative exploration and ideation for fbtax.cloud FASE 2"
        agent: "analyst"
        command: "/bmad:cis:workflows:brainstorming"
      - id: "research"
        status: "required"
        note: "Research on NT 2025.002, RFB norms, ERP integration strategies"
        agent: "analyst"
        command: "/bmad:bmm:workflows:research"

  # PHASE 2: Planning (Required)
  - phase: "2"
    name: "Planning"
    workflows:
      - id: "prd"
        status: "required"
        note: "Product Requirements Document with FRs and NFRs"
        agent: "pm"
        command: "/bmad:bmm:workflows:prd"
      - id: "create-ux-design"
        status: "conditional"
        note: "if_has_ui - Determine after PRD"
        agent: "ux-designer"
        command: "/bmad:bmm:workflows:create-ux-design"

  # PHASE 3: Solutioning (Required)
  - phase: "3"
    name: "Solutioning"
    workflows:
      - id: "create-architecture"
        status: "required"
        note: "System architecture including security, multi-tenancy, ERP integrations"
        agent: "architect"
        command: "/bmad:bmm:workflows:create-architecture"
      - id: "create-epics-and-stories"
        status: "required"
        note: "Break down PRD into implementable epics and stories with full context"
        agent: "pm"
        command: "/bmad:bmm:workflows:create-epics-and-stories"
      - id: "test-design"
        status: "recommended"
        note: "System-level testability review for motor de apuração"
        agent: "tea"
        command: "/bmad:bmm:workflows:testarch-test-design"
      - id: "implementation-readiness"
        status: "required"
        note: "Validates PRD + Architecture + Epics + UX before implementation"
        agent: "architect"
        command: "/bmad:bmm:workflows:check-implementation-readiness"

  # PHASE 4: Implementation (Required)
  - phase: "4"
    name: "Implementation"
    workflows:
      - id: "sprint-planning"
        status: "pending"
        note: "Creates sprint plan - subsequent work tracked there"
        agent: "sm"
        command: "/bmad:bmm:workflows:sprint-planning"
